package com.walmart.move.nim.receiving.rc.contants;

/** Enum for different action types passed in receipt event */
public enum ActionType {
  RECEIPT,
  RECEIPT_UPDATE,
  ITEM_MISSING,
  ITEM_MISSING_RECEIPT,
  ITEM_MISSING_UPDATE
}
